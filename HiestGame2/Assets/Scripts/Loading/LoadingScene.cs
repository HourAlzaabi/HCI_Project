using UnityEngine;

public class LoadingScene : MonoBehaviour
{
    // Intentionally empty - this scene is just a transition point
    // PhotonNetwork.AutomaticallySyncScene will handle moving 
    // both clients to the next scene when master calls LoadLevel
}